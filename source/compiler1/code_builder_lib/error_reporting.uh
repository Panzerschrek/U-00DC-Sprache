import "/random_access_range.u"
import "/code_builder_errors.uh"
?macro <? REPORT_ERROR:block ( ?names_scope:expr, ?error_code:ident, ?file_pos:expr ) ?> ->
<?
	{
		auto ??container= ?names_scope.GetErrors();
		auto mut ??lock= ??container.lock_mut();
		??lock.get_ref().push_back( ReportError( CodeBuilderErrorCode::?error_code, ?file_pos ) );
	}
?>

namespace U
{

fn ReportError( CodeBuilderErrorCode code, FilePos file_pos ) : CodeBuilderError;

} // namespace U
