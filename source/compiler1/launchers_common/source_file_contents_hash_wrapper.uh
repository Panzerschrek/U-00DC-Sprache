import "funcs_u.uh"

type CalculateSourceFileContentsHashCallback= fn( UserHandle data,  U1_StringView& hash );

fn nomangle U1_CalculateSourceFileContentsHash(
	U1_StringView& contents,
	CalculateSourceFileContentsHashCallback callback,
	UserHandle callback_data ) unsafe;
