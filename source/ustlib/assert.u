// TODO - add message
?macro <? assert:block ( ?e:expr ) ?term:opt<? ; ?> ?> ->
<?
{
	halt if( ?e );
}
?>

// TODO - add message
?macro <? debug_assert:block ( ?e:expr ) ?term:opt<? ; ?> ?> ->
<?
{
	if( compiler::options::generate_debug_info || compiler::options::optimization_level == "0"c8 )
	{
		assert( ?e )
	}
}
?>
