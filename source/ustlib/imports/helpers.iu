namespace ust
{

// Use this function to supress "unused name" error for variables.
template</type T/>
fn ignore_unused(T& t)
{
	// Force use unused variable in "typeof" context.
	static_if( same_type</ typeof(t), T /> ) {}
}

} // namespace ust
