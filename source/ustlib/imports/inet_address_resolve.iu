import "inet_address.iu"
import "vector.iu"

namespace ust
{

// Converts address name (usually domain name) into ip address (ipv4 or ipv6).
// Multiple addresses may be returned.
// In case of fail returns empty vector.
// Internally it uses system-specific name resolution mechanism, like DNS, hosts file, etc.
fn resolve_inet_address( string_view8 name ) : vector</ip_address/>;

} // namespace ust
