import "c.iu"
import "b.iu"
import "a.iu"
import "y.iu"

fn YSetABC(u32 a, u32 b, u32 c)
{
	unsafe
	{
		VarA()= a;
		VarB()= b;
		VarC()= c;
	}
}

fn YGetABC() : [u32, 3]
{
	unsafe
	{
		var [u32, 3] a[VarA(), VarB(), VarC()];
		return a;
	}
}
