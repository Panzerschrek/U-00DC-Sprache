import "/qwerty/some.iu"
import "/qwe/rty/another.iu"
import "/ach/wie/gut/dass/niemand/weiss/wer/ich/bin/und/wie/ich/heiss/rumpelstilzchen.iu"
import "/d/krupps.iu"
import "/root_import.iu"

fn nomangle main() call_conv( "C" ) : i32
{
	auto a= AFunc( 77 );
	halt if( a != 154 );

	auto b= BFunc( 32 );
	halt if( b != 16 );

	auto c= CFunc( 3 );
	halt if( c != 27 );

	auto d= DFunc( 986 );
	halt if( d != 200 );

	auto e= EFunc( 7 );
	halt if( e != 28 );

	return 0;
}
