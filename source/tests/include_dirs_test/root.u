import "/aa.iu" // Should find aa, bb, cc in their directories
import "/bb.iu"
import "/cc.iu"
import "/0.iu" // should found b/0.iu ( because order of include dirs is "a", "b", "c" )

static_assert( aa == 9854 );
static_assert( bb == -9863541 );
static_assert( cc == 1125417 );
static_assert( zero == 6666 );

fn nomangle main() call_conv( "C" ) : i32
{
	return 0;
}
