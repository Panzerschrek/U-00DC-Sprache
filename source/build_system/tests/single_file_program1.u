import "/stdout.iu"
import "/string_conversions.iu"
import "single_file_program_import.iu"

fn nomangle main() call_conv( "C" ) : i32
{
	ust::stdout_print( ust::concat( "Single file program 1: ", ust::to_string8( GetX() ), "!\n" ) );
	return 0;
}
