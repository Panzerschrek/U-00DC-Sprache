import "/stdout.iu"
import "/string_conversions.iu"
import "/lib/lib.uh"

fn nomangle main() call_conv( "C" ) : i32
{
	ust::stdout_print( "Div result: " + ust::to_string8( Div( 76u, 5u ) ) + "\n" );
	return 0;
}
