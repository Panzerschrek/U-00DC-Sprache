import "/two_returner/two_returner.iu"
import "/integer_adder/integer_adder.iu"

fn nomangle main() call_conv( "C" ) : i32
{
	halt if( GetTwo() != 2 );
	halt if( AddInts( 7, 25 ) != 32 );
	return 0;
}
