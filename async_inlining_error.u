class SomeClass
{
public:
	fn constructor();

	fn async Foo( this ) : size_type
	{
		auto& s= x_;
		return 0s;
	}

private:
	i32 x_;
}

fn Run()
{
	auto f= Func();
}

fn async Func()
{
	var SomeClass mut c;
	auto res= c.Foo().await;
}
